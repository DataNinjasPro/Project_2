def mash(first_string, second_string):
    """Mashes two strings together."""
    mashed = first_string + second_string
    return mashed

def mash(first_string, second_string="."):
    """Mashes two strings together."""
    mashed = first_string + second_string
    return mashed

